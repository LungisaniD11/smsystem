@{
    ViewData["Title"] = "Access Denied";
}

<section class="mt-5">
    <h2>Access denied</h2>
    <p>Your account was authenticated, but it is not in the administrator allow-list.</p>
    <a class="btn btn-outline-primary" asp-controller="Account" asp-action="Login">Return to Login</a>
</section>
